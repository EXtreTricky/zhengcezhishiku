# 4201 综合优化热修（2026-09-08）

## 这次一起解决的问题

1. 一键巡检跑完后可能自动开启新一轮
2. 页面刷新后“停止巡检”失效
3. Windows 下 SIGTERM 杀不干净 Node/子进程树
4. cron / 自动补偿 / 手动巡检可能互相抢任务
5. 全量巡检单格搜索与 AI 请求过重，容易表现为“卡死”
6. 来源 404/timeout/DNS/解析失败以前容易被吞掉，健康状态不够可信
7. 来源健康面板只有状态，没有明确下一步动作
8. 飞书 / LLM / 后台 API 慢请求会让界面长期等待

## 新行为

### 手动巡检
- 点一次只执行当前这一轮
- 当前任务跑完即停
- 不会自动 reset 开下一轮
- 全量巡检只跑本轮剩余任务
- 大批量巡检自动禁用逐格 AI 扩词，异常省再用 deep 补偿

### 停止巡检
- 不再依赖浏览器内存中的 runId
- 页面刷新后仍可停止
- `/api/crawl/sweep-stop` 可停止手动/cron/补偿任务
- Windows 使用 `taskkill /T /F` 杀完整进程树
- 页面等后端锁真正释放后才显示“已停止”

### 调度
- 全局 `data/.sweep.lock` 保证同一时间仅一套 sweep
- owner 区分 manual / cron / compensation
- cron 只有显式 `--new-cycle` 才能在上一周期全部完成后开启下一周期
- 手动全量不会触发 new-cycle
- 巡检超时会结束整个进程树

### 搜索效率
- gov.cn 政策库 + 1 条地区定向 Bing 优先
- 结果不足才增加额外搜索
- 搜索计划有限并发
- 全量巡检不逐格调用 AI 扩词
- 真正异常来源通过 deep compensation 做深搜

### 来源问题中心
原“来源健康监控”改为更可操作的“来源问题中心”：
- 默认只展示真实异常省份
- 显示最近成功/发现时间
- 显示搜索/官网枚举数量
- 显示失败入口数和真实错误
- 显示补偿任务状态
- 直接给出建议动作
- 支持“探测”和“修复复检”

## 验证

- crawler regression: 18/18 passed
- 关键 JS: node --check 全通过
- 网络异常 source-probe：可以在有限时间内返回明确 EAI_AGAIN/timeout 诊断，不无限挂起

## 安全

本补丁不包含：
- `.env`
- `data/db.json`
- `node_modules`

因此不会覆盖你本机现有 155+ 待审批、云南 sourceHealth、补偿队列或真实密钥。

## 本机覆盖后

保留原 `.env` 和 `data/db.json`，重启 4201。

建议验证：
1. 打开 `/admin/`
2. 点“一键巡检”跑 5 个组合
3. 中途点“停止当前巡检”，应真正停止
4. 再点一次，跑完后应停住，不会自动再开下一轮
5. 用“来源问题中心”探测一个异常省
6. `node test/crawler-regress.js` 应显示 18/18 passed
